#ifndef WO_COULOMB_SOLVER_CPU_H
#define WO_COULOMB_SOLVER_CPU_H

// CPU solver implementations are currently header-only in solver.h.
// This shim keeps includes explicit for CPU-only code and tests.
#include "solver.h"

#endif  // WO_COULOMB_SOLVER_CPU_H
